---
import { ThreadDetailPage } from '@/components'
import Layout from '@/layout.astro'

export const prerender = false

const { id } = Astro.params
---

<Layout>
    <ThreadDetailPage client:load threadId={id!} />
</Layout>
